package com.example.quizeforkideos;


public final class quizAppUtils {

    //This method increments score count on each correct selection/answer from user entry
    static int scoreCount() {
        int score = +1;
        return score;
    }

    //This method return zero count on each incorrect selection/answer from user entry
    int zeroCount() {

        int score = +0;
        return score;
    }

}
