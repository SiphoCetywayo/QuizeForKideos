<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.quizeforkideos">

    <supports-screens
        android:anyDensity="true"
        android:largeScreens="true"
        android:normalScreens="true"
        android:resizeable="true"
        android:smallScreens="true"
        android:xlargeScreens="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/eduicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".QuestionFive" android:configChanges="orientation|screenSize"></activity>
        <activity android:name=".QuestionFour" android:configChanges="orientation|screenSize"/>
        <activity android:name=".QuestionThree" android:configChanges="orientation|screenSize"/>
        <activity android:name=".FinalScore" android:configChanges="orientation|screenSize"/>
        <activity android:name=".QuestionTwo" android:configChanges="orientation|screenSize"/>
        <activity
            android:name=".QuestionOne"
            android:configChanges="orientation|screenSize" />
        <activity android:name=".QuizMenuSelect" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>